# ruby-script-template

## Getting started

Build image with docker

```bash
docker-compose build
```

Run a container

```bash
docker-compose run --rm bash
```

Verify `rspec`

```bash
bundle exec rspec
```
